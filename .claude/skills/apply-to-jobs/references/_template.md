# <Platform name> — application form notes

Accumulated opportunistically by the `apply-to-jobs` skill, one entry per run. Don't trust these
blindly — sites change; verify against the live page and update the entry if it's stale.

## <YYYY-MM-DD> — <company>, <job title>

- URL pattern: ...
- Fields seen / how they mapped to the profile: ...
- Quirks: (multi-step flow? resume parses into other fields automatically? salary is a range?
  requires an account/login first? EEO questions phrased unusually?)
- Anything that couldn't be filled and why: ...

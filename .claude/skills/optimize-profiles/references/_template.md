# <Platform name> — profile-editing notes

Accumulated opportunistically by the `optimize-profiles` skill, one entry per run. Don't trust
these blindly — sites change; verify against the live page and update the entry if it's stale.

## <YYYY-MM-DD> — <what was edited>

- URL pattern / how to reach the edit UI for this section: ...
- Fields seen / how they mapped to the profile: ...
- Quirks: (modal vs inline edit? character limits? does saving notify the user's network? does
  `computer` `type` drop characters — use `javascript_tool` + native setter if so?)
- Anything that couldn't be edited and why: ...
